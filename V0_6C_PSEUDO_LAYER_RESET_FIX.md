# 0.6c Pseudo Layer Reset Fix

- Przywrócono oryginalny układ nicków i wyników.
- Nie są dodawane żadne nowe elementy do wierszy rankingu.
- `::before` pokazuje tło 6 px wewnątrz.
- `::after` pokazuje ramkę na pełnym rozmiarze.
- Ramka i tło korzystają z osobnych map tekstur.
- Paczka nie zawiera PNG ani folderu tekstur.
